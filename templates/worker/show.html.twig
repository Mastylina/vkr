{% extends 'base.html.twig' %}

{% block title %}Worker{% endblock %}

{% block body %}
    <h1>Worker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ worker.email }}</td>
            </tr>
            <tr>
                <th>NumberPhone</th>
                <td>{{ worker.numberPhone }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>  <img src="{{ worker.photo }}" class="card-img-top" alt="..."></td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ worker.surname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ worker.name }}</td>
            </tr>
            <tr>
                <th>Patronymic</th>
                <td>{{ worker.patronymic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_worker_index') }}">back to list</a>

    <a href="{{ path('app_worker_edit', {'id': worker.id}) }}">edit</a>

    {{ include('worker/_delete_form.html.twig') }}
{% endblock %}
